This resource directory is intentionally kept minimal for Slice 1.

Real server route search/detail endpoints are not available yet. The app uses
`MockRemoteRouteClient` and JSON/GPX test fixtures until the service contract is
connected.
